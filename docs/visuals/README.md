# Encounter dossier

## Capture notes

Actual React browser captures with the built-in DemoRiotClient/DemoLiveClient providers and a temporary SQLite database. The hero's encounter file is explicitly illustrative. Demo names and scores do not refer to real investigated players.

Web captures use Chromium at 1440 × 1080 (desktop) and 390 × 1080 (mobile), with reduced motion enabled. Screenshots are real rendered interfaces, not image-generated UI mockups. Raster renderer examples retain their native dimensions.

## Verification — 2026-09-14

`npm run verify` passed; the backend suite passed 42 tests. Browser checks exercise demo scanning and responsive layouts. Real Riot and Live Client integrations were not called.

Only the existing visual surfaces were changed. The screenshots are not evidence of end-to-end service availability, accessibility certification, or production performance.
